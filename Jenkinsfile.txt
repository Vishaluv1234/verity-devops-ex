pipeline {
    agent any
    stages {
		
		
		// ******ALL CODE TO BE ADDED BELOW THIS COMMENT******
		
//Clean stage dev
        stage('Clean') {
            steps {
                sh 'mvn clean'
            }
        }
		// Cleanstage

//Code starts for stage Compile
		stage('Compile') {
            steps {
                sh 'mvn compile'
            }
        }
		//Code ends for new stage Compile

//Code starts for Code Analysis
		stage('Code Analysis') {
            steps {
			 script {
          			scannerHome = tool 'sonar'
        			}
				withSonarQubeEnv('SonarQubeServer')
				{
				sh "${scannerHome}/bin/sonar"
				}
			}
		}
		//Code ends for stage Code Analysis


	
		// ******ALL CODE TO BE ADDED ABOVE THIS COMMENT******
    }
}